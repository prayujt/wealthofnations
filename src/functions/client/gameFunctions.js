const {
	createCollection,
	dropCollection,
	remove,
	removeAll,
	get,
	getAll,
	insert,
	exists,
	update,
	updateField,
	replace,
	watch,
	match,
	dropDatabase,
} = require('../../global');

exports.clientGameFunctions = async (client, socket) => {};
